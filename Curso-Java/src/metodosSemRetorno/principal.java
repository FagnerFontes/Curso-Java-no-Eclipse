package metodosSemRetorno;

public class principal {
	
	// Métodos sem retorno, também conhecidos como métodos 'void', são usados quan se deseja que o métodos execute uma ação mas não precise devolver nenhum valor. O ripo de retorno 'void' indica que o método não retorna nada
	public static void main (String[] args) {
		
		Calculadora calc = new Calculadora();
		
		calc.imprimeSoma(5, 3);
		
		
	}

}
